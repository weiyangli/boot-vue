package com.liweiyang.lwyFrame.util.base.algorithm;

/**
 * 递归
 *
 */
public class Recursion {

    /**
     * 一个字符串的所有可能得组合方式
     * @param str
     */
    public void strMay(String str) {
        char [] chars = str.toCharArray();
        for (int i =0; i < chars.length; i++) {
            char first = chars[i];
        }
    }
}
